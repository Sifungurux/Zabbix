---
- hosts: all
  become: yes
  become_method: sudo
  gather_facts: yes
  vars:
          - mysql_root_db_pass: 1234
  roles:
          - {role: ansible-mysql, mysql_db: [{name: zabbix}], mysql_users: [{name: zabbix_service, pass: pass_zabbix, priv: "*.*:ALL"}]}
          - ansible-zabbix 
